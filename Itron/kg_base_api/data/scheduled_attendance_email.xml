<odoo>
    <data noupdate="0">

        <!--        <record id="ir_cron_daily_attendance_email" model="ir.cron">-->
        <!--            <field name="name">Daily Attendance Email</field>-->
        <!--            <field name="model_id" ref="model_early_late_request"/>-->
        <!--            <field name="state">code</field>-->
        <!--            <field name="code">model.send_weekly_late_arrival_report()</field>-->
        <!--            <field name="interval_number">1</field>-->
        <!--            <field name="interval_type">weeks</field>-->
        <!--            &lt;!&ndash;            <field name="nextcall">2024-12-16 14:00:00</field>&ndash;&gt;-->
        <!--            <field name="numbercall">-1</field>-->
        <!--        </record>-->
        <record id="ir_cron_daily_attendance_email" model="ir.cron">
            <field name="name">Daily Attendance Email</field>
            <field name="model_id" ref="hr_attendance.model_hr_attendance"/>
            <!--            <field name="user_id" ref="base.user_admin"/>-->
            <field name="state">code</field>
            <field name="code">model.send_daily_attendance_email()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="doall">False</field>
            <field name="active">True</field>
            <!--            <field name="nextcall" eval="(DateTime.now() + relativedelta(hours=1)).strftime('%Y-%m-%d %H:%M:%S')"/>-->
        </record>
        <record id="ir_cron_send_categorized_attendance_email" model="ir.cron">
            <field name="name">Daily Categorized Attendance Email</field>
            <field name="model_id" ref="hr_attendance.model_hr_attendance"/>
            <!--            <field name="user_id" ref="base.user_admin"/>-->
            <field name="state">code</field>
            <field name="code">model.send_categorized_attendance_email()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="doall">False</field>
            <field name="active">True</field>
        <field name="nextcall" eval="(DateTime.now().replace(hour=4, minute=45) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')" />
        </record>


    </data>
</odoo>
