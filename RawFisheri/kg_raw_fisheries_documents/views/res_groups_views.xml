<odoo>

    <record id="kg_view_groups_form_inherit" model="ir.ui.view">
        <field name="name">KG Groups form inherit</field>
        <field name="model">res.groups</field>
        <field name="inherit_id" ref="base.view_groups_form"/>
        <field name="arch" type="xml">
            <field name="share" position="after">
                <field name="is_documents_groups"/>
            </field>
        </field>
    </record>

    <record id="action_kg_documents_res_groups" model="ir.actions.act_window">
        <field name="name">Document Groups</field>
        <field name="res_model">res.groups</field>
        <field name="context">{'search_default_filter_no_share': 1, 'ir.ui.menu.full_list':
            1,'default_is_documents_groups':True}
        </field>
        <field name="domain">[('is_documents_groups','=',True)]</field>
        <field name="help">A group is a set of functional areas that will be assigned to the user in order to give them
            access and rights to specific applications and tasks in the system. You can create custom groups or edit the
            ones existing by default in order to customize the view of the menu that users will be able to see. Whether
            they can have a read, write, create and delete access right can be managed from here.
        </field>
    </record>

    <menuitem id="documents_groups_menu" action="action_kg_documents_res_groups" parent="documents.Config"
              sequence="8"/>


</odoo>