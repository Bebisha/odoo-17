<?xml version ="1.0" encoding="utf-8" ?>
<odoo>
    <data>
        <!-- mail send to users cheque date before 5 days -->
        <record id="mail_send_to_users_scheduler" model="ir.cron">
            <field name="name">Pdc due email send to users</field>
            <field eval="True" name="active"/>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall"
                   eval="(DateTime.now().replace(hour=0, minute=0, second=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
            <field name="numbercall">-1</field>
            <field name="model_id" ref="sh_pdc.model_pdc_wizard"/>
            <field name="state">code</field>
            <field name="code">model.action_send_mail_to_users()</field>
            <field name="priority" eval="5"/>
        </record>
    </data>
</odoo>