<odoo>
    <data>
        <record id="view_select_multi_expenses_form" model="ir.ui.view">
            <field name="name">select.multi.expenses.form</field>
            <field name="model">multi.expenses.wizard</field>
            <field name="type">form</field>
            <field name="arch" type="xml">
                <form string="Add: Expense Lines">
                    <group>
                        <group>
                            <field name="sheet_id" readonly="1" force_save="1"/>
                            <field name="employee_id" readonly="1" force_save="1"/>
                            <field name="company_id" readonly="1" force_save="1"/>
                        </group>
                        <notebook>
                            <page string="Add: Expense Lines">
                                <field name="expense_ids"
                                       domain="[('state','not in',['done','refused']),('employee_id','=',employee_id),('company_id','=',company_id)]">
                                    <tree editable="bottom" multi_edit="1" sample="1" js_class="hr_expense_tree"
                                          decoration-info="state == 'draft'">
                                        <field name="is_select"/>
                                        <field name="date" readonly="1" force_save="1"/>
                                        <field name="name" readonly="1" force_save="1"/>
                                        <field name="company_id" column_invisible="1"/>
                                        <field name="employee_id" readonly="1" force_save="1"/>
                                        <field name="payment_mode" readonly="1" force_save="1"/>
                                        <field name="total_amount" readonly="1" force_save="1"/>
                                        <field name="state"
                                               decoration-info="state in ['draft', 'reported']"
                                               decoration-success="state in ['approved', 'done']"
                                               decoration-warning="state == 'submitted'"
                                               decoration-danger="state == 'refused'" widget="badge"/>
                                    </tree>
                                </field>
                            </page>
                        </notebook>
                    </group>
                    <footer>
                        <button name="select_expenses" string="Add" type="object" class="oe_highlight"/>
                        <button string="Cancel" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>
    </data>
</odoo>